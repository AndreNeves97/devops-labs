# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
.terraformrc
terraform.rc

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
*tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
terraform/terraform.log
terraform/variables.tf

# Environment variables and secrets
.env
.env.local
.env.*.local
*.env

# AWS credentials
.aws/
*.pem
*.key
*.p12
*.pfx
credentials
config.json

# Terraform backend config (may contain sensitive values)
*.tfbackend
terraform.tfbackend

# Kubernetes config (may contain sensitive tokens)
.kube/
kubeconfig
*.kubeconfig

# Node modules (if not already ignored)
node_modules/
